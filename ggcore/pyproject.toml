[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "ggcore"
version = "0.1.0"
description = "Native (Rust) core for the GG browser engine: HTML parsing, CSS parsing, style computation"
requires-python = ">=3.9"

[tool.maturin]
module-name = "ggcore"
